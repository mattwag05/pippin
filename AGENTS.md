# Agent Instructions

All agent guidance for this repo lives in **[CLAUDE.md](CLAUDE.md)** — the single source of truth: commands, architecture, build/CI gates, per-subsystem gotchas, the beads (`bd`) issue-tracking workflow, session-completion rules, and the `graphify-out/` onboarding knowledge graph. Read it first.

New here? Start with [`graphify-out/GRAPH_REPORT.md`](graphify-out/GRAPH_REPORT.md), or query the graph instead of grepping: `graphify query "how does an MCP tool call reach a bridge?"`.
