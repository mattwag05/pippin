name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    # Self-hosted runner label (matches ci.yaml). `macos-15` is a GitHub-hosted
    # label and won't schedule on the Forgejo self-hosted `macos` runner.
    runs-on: macos-tart

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Run tests
        run: swift test

      - name: Build release tarball
        run: make tarball

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Extract changelog section
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk "/^## \[$VERSION\]/{found=1; next} found && /^## \[/{exit} found{print}" CHANGELOG.md > release-notes.md

      - name: Determine pre-release
        id: prerelease
        run: |
          if [[ "$GITHUB_REF_NAME" == *"-alpha"* || "$GITHUB_REF_NAME" == *"-beta"* ]]; then
            echo "is_prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Forgejo release
        id: create_release
        env:
          FORGEJO_TOKEN: ${{ secrets.FJ_API_TOKEN }}
          TAG_NAME: ${{ github.ref_name }}
          VERSION: ${{ steps.version.outputs.version }}
          IS_PRERELEASE: ${{ steps.prerelease.outputs.is_prerelease }}
        run: |
          NOTES=$(cat release-notes.md)
          RESPONSE=$(curl -s -X POST \
            "${{ vars.FJ_API_BASE }}/repos/${{ github.repository }}/releases" \
            -H "Content-Type: application/json" \
            -H "Authorization: token $FORGEJO_TOKEN" \
            -d "{
              \"tag_name\": \"$TAG_NAME\",
              \"name\": \"$TAG_NAME — pippin\",
              \"body\": $(echo "$NOTES" | jq -Rs .),
              \"prerelease\": $IS_PRERELEASE
            }")
          echo "release_id=$(echo "$RESPONSE" | jq -r '.id')" >> "$GITHUB_OUTPUT"

      - name: Upload tarball to Forgejo release
        env:
          FORGEJO_TOKEN: ${{ secrets.FJ_API_TOKEN }}
          RELEASE_ID: ${{ steps.create_release.outputs.release_id }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          TARBALL=".build/release-artifacts/pippin-${VERSION}-arm64-macos.tar.gz"
          curl -s -X POST \
            "${{ vars.FJ_API_BASE }}/repos/${{ github.repository }}/releases/${RELEASE_ID}/assets" \
            -H "Authorization: token $FORGEJO_TOKEN" \
            -F "attachment=@$TARBALL"
